<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermall.backend.domain.order.mapper.OrderItemMapper">
    <insert id="insertBatch">
        INSERT INTO order_items (
            order_id, product_id, merchant_id, product_snapshot, quantity, price, created_at
        ) VALUES
        <foreach collection="items" item="item" separator=",">
            (
                #{item.orderId}, #{item.productId}, #{item.merchantId}, 
                #{item.productSnapshot}, #{item.quantity}, #{item.price}, 
                NOW()
            )
        </foreach>
    </insert>
</mapper> 