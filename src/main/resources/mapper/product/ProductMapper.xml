<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermall.backend.domain.product.mapper.ProductMapper">
    <resultMap id="ProductVOMap" type="com.supermall.backend.domain.product.vo.ProductVO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="subtitle" column="subtitle"/>
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="brandId" column="brand_id"/>
        <result property="brandName" column="brand_name"/>
        <result property="mainImage" column="main_image"/>
        <result property="subImages" column="sub_images"/>
        <result property="detail" column="detail"/>
        <result property="price" column="price"/>
        <result property="stock" column="stock"/>
        <result property="status" column="status"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <select id="pageProducts" resultMap="ProductVOMap">
        SELECT 
            p.*,
            c.name as category_name,
            b.name as brand_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id 
            AND c.deleted = 0 
            AND c.status = 1
        LEFT JOIN brand b ON p.brand_id = b.id 
            AND b.deleted = 0 
            AND b.status = 1
        WHERE p.deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (
                p.id = #{keyword}
                OR p.name LIKE CONCAT('%', #{keyword}, '%')
                OR p.subtitle LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        ORDER BY p.created_time DESC
    </select>
</mapper> 