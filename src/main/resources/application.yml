spring:
  datasource:
    url: jdbc:mysql://139.196.82.162:3306/super-mall?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: jacl
    password: jacl
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ??????? `update`????????? `validate` ? `none`
    properties:
      hibernate:
        format_sql: true  # ??? SQL ??
        use_sql_comments: true  # ?? SQL ??
    show-sql: true  # ?? SQL

  mybatis-plus:
    configuration:
      map-underscore-to-camel-case: true  # ????????
    global-config:
      db-config:
        logic-delete-value: 1  # ??????
        logic-not-delete-value: 0  # ?????

  server:
    port: 8080  # ????

  logging:
    level:
      root: INFO  # ????????
      org.springframework.web: DEBUG  # ?? Spring Web ????
      com.supermall: DEBUG  # ??????????
